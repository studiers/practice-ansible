sudo: required

services:
- docker
- docker-compose

before_install:
- apt-get update
- docker-compose -v
- apt-get upgrade docker-compose

script:
- docker-compose up -d
- docker-compose exec ansible-master ansible all -m ping